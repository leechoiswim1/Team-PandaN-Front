import React from "react";
import Template from "../components/Template";

const Home = ({ history }) => {
  return (
    <React.Fragment>
      <Template />
      <div style={{ fontSize: "2.0rem" }}>Home</div>
    </React.Fragment>
  );
};

export default Home;
